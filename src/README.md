# Live code

What this file is: the guide to the project’s reusable analysis code. Who writes it: your AI. When you read it: when you want to know what code is active or how to run it.

`src/` is the only place for live code. Extend a file here instead of making a second live copy. Each experiment stores a frozen copy of the exact code that ran.

- [analyze_steps.py](analyze_steps.py) compares weekday and weekend step counts. *(example — your AI will replace this)*
